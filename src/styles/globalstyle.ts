import { StyleSheet } from "react-native"

export const colors = {
    black: '#000',
    white: '#FFF',
    red: '#A91C25',
    grey: '#343434',
    whitebackground: 'rgba'
}
export const styleContainer = StyleSheet.create({
	container: {
		flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    }
})
